services:
  mongodb:
    image: mongo:7.0
    container_name: mongodb_envarch
    # Reinicia automáticamente al arrancar Docker o tras fallos, excepto si se detuvo manualmente
    restart: unless-stopped
    ports:
      - "127.0.0.1:27018:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - app-network

  backend:
    build:
      context: ./envArch
      dockerfile: Dockerfile
    container_name: backend_envarch
    # Reinicia automáticamente al arrancar Docker o tras fallos, excepto si se detuvo manualmente
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/envarchdb
      - PORT=8080
      - JWT_SECRET=9a2f64a13e5f2a1b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b
      - UPLOAD_DIR=/app/uploads
    depends_on:
      - mongodb
    volumes:
      - backend_uploads:/app/uploads
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend_envarch
    # Reinicia automáticamente al arrancar Docker o tras fallos, excepto si se detuvo manualmente
    restart: unless-stopped
    ports:
      - "7459:7459"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    networks:
      - app-network

  audit-service:
    build:
      context: ./auditService
      dockerfile: Dockerfile
    container_name: audit_envarch
    # Reinicia automáticamente al arrancar Docker o tras fallos, excepto si se detuvo manualmente
    restart: unless-stopped
    ports:
      - "127.0.0.1:8082:8082"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/auditdb
      - PORT=8082
    depends_on:
      - mongodb
    networks:
      - app-network

  xmlends-service:
    build:
      context: ./xmlendsService
      dockerfile: Dockerfile
    container_name: xmlends_envarch
    # Reinicia automáticamente al arrancar Docker o tras fallos, excepto si se detuvo manualmente
    restart: unless-stopped
    ports:
      - "127.0.0.1:8083:8083"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/xmlendsdb
      - PORT=8083
      - JWT_SECRET=9a2f64a13e5f2a1b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b
      - UPLOAD_DIR=/app/uploads_xmlends
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    depends_on:
      - mongodb
    volumes:
      - xmlends_uploads:/app/uploads_xmlends
    networks:
      - app-network

  gateway:
    image: nginx:alpine
    container_name: gateway_envarch
    # Reinicia automáticamente al arrancar Docker o tras fallos, excepto si se detuvo manualmente
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./gateway/gateway.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
      - frontend
      - audit-service
      - xmlends-service
    networks:
      - app-network

volumes:
  mongo_data:
  backend_uploads:
  xmlends_uploads:

networks:
  app-network:
    driver: bridge
